# 自动化多窗口项目总控架构

## v1.11.0 远程验证架构

本地与远程验证统一进入 `tests/run-validation.ps1`，避免在 GitHub Actions YAML 中复制测试顺序或漏掉校验阶段。入口按语法、Schema、Skill、Pester 的固定顺序失败即停，并把摘要与 NUnit XML 写入被 Git 忽略的 `TestResults/`；Python 依赖由 `tests/requirements-validation.txt` 锁定，Pester 在 CI 中固定为兼容现有测试语法的 4.10.1，并使用 Windows PowerShell 5.1 执行。

`ci.yml` 只读取仓库并验证 PR 与 `main`，且以 `always()` 上传已经产生的测试证据。`release-validation.yml` 只在 `v*` 标签上运行，先证明标签与 Skill 版本一致，再重复完整验证和安装包构建；`verify-release-package.ps1` 随后重新计算外部 SHA-256，并逐项复核包内清单的大小与哈希，最后上传短期 Artifact。两个工作流都使用最小 `contents: read` 权限，不自动创建 Release、推送提交或修改分支保护规则。

## v1.10.0 已有项目接入预检

`test-project-readiness.ps1` 在任何状态初始化之前只读检查既有项目，并输出符合 `project-readiness.schema.json` 的 JSON。它不把报告写入目标项目，不修改 Git 配置或忽略规则，也不创建分支和工作树。报告包含 15 分钟有效期，保存位置由调用方选择。

已有 Git 项目的 `initialize` 必须接收 `-PreflightPath`。`assert-project-readiness.ps1` 先验证报告身份、项目路径、目标分支、有效期和 `compatible` 结论，再执行一次实时只读预检并核对仓库根、HEAD、当前分支、工作区、未完成 Git 操作、工作树清单和状态目录属性。任一变化都在创建 `.codex-orchestrator` 前停止。验证通过后，报告路径、SHA-256 和关键 Git 身份写入 `workflow.json`；非 Git 的只读分析项目保持原有初始化兼容入口。

报告根据阻塞项和警告推荐完整集成、隔离开发、只读或阻塞模式。项目不存在、非 Git 仓库、无 HEAD、非仓库顶层、detached HEAD、未完成 Git 操作、冲突、Git 锁或已跟踪的状态目录会关闭接入门禁。脏工作区、目标分支不匹配、Git 身份缺失、状态目录未忽略以及特殊仓库能力会降低推荐模式，必须由总控向用户说明后另行处理。

## 目标

让一个 Codex 总控任务读取同一项目的任务状态和结构化输出，并在授权范围内派发后续工作，形成可审计、可恢复、可停止的自动工作流。

## 模块

1. **总控 Skill**：理解目标并执行调度规则。
2. **项目分析器**：只读检查目录、文档、Git、调用链和测试。
3. **任务规划器**：拆分任务、建立依赖并分配文件归属。
4. **任务连接层**：通过 Codex 原生接口列出、读取、发送和等待任务。
5. **状态存储**：保存任务 ID、阶段、基线、证据和事件。
6. **结果验证器**：校验协议、版本、修改范围和测试证据。
7. **阶段门禁**：决定继续、返修、等待或停止。
8. **汇报器**：生成进度与最终交付报告。
9. **状态管理器**：通过项目锁、原子替换和只追加事件日志执行确定性状态迁移。
10. **回传握手层**：让执行任务主动唤醒总控，并用持久化事件和 ACK 证明交付。

## 数据流

用户目标与授权 → 项目分析 → 任务规划 → 任务派发 → 原始结果保留 → 总控规范化 JSON → Schema 与证据验证 → 阶段门禁 → 下一任务或最终报告。

## 状态模型

普通任务：`draft → awaiting_approval → approved → dispatched → running → verifying → completed`

开发任务受控提交：`running → awaiting_commit → verifying → completed`。其中 Git 子状态独立记录为 `not-started → worktree-ready → changes-ready → staged → committed → merged`，用于恢复中断操作，不能替代任务结果验证和回传 ACK。

任意阶段可以进入 `blocked`、`failed`、`cancelled` 或 `stale`。终止状态不得自动恢复；恢复必须重新核对外部实际状态。

## 架构约束

- Skill 源码与目标项目运行状态分离。
- 任务 ID 是身份依据，标题只用于显示。
- 修改任务必须声明文件归属；归属冲突时禁止并行。
- Git 基线和结果修订必须一致，否则结果过期。
- 执行任务不负责生成严格 JSON；总控保留原文并负责规范化，缺失事实不得猜测。
- 分析、实施、测试、审查逐级通过，不能用后级结果补写前级授权。
- 失败只允许一次有明确原因的定向返修，连续修补必须停止重规划。

## 第一版边界

包含原生任务接口调度、结构化状态、依赖和冲突检查、Git Worktree 绑定门禁、恢复与汇报。不包含鼠标自动化、后台常驻服务、图形看板、自动提交推送或生产部署。

## Git Worktree 隔离

工作流状态只保存在主项目的 `.codex-orchestrator` 中。使用真实提交作为基线的开发任务必须绑定独立工作树；绑定证据记录仓库根目录、绝对路径、分支、HEAD、干净状态和检查时间，并以 SHA-256 固定。派发前重新读取 Git 状态，发现共享路径、共享分支、detached HEAD、脏文件、错误仓库或基线漂移时关闭门禁。v1.5 只验证和绑定已经存在的工作树，不负责创建、删除、提交、合并或推送。

## v1.6 五窗口 E2E 证据门禁

完整验收由一个总控任务和分析、开发、测试、审查四个不同执行任务组成。开发在分支工作树产生一个新提交；测试与审查在互不共享的验证工作树检查同一提交，验证工作树允许 detached HEAD。E2E 验证器要求四个任务均具备可信验证和回传 ACK，测试至少包含一个退出码为 0 的通过项，审查不得保留未解决发现。自动测试只能证明验证器行为，不能冒充真实 Codex 多窗口结果。

## v0.2 状态层

`manage-workflow.ps1` 是运行状态的唯一写入口。`workflow-state.psm1` 负责项目级独占锁、同目录原子替换、事件序号、角色依赖、结果证据和状态迁移。`events.jsonl` 只追加，`audit` 用于检测序号断裂、工作流不一致和中断残留文件。

结果层采用双输出：`result.json` 是严格的机器交换格式，`validate-result.ps1` 检查字段类型和 Git 基线；仅在校验通过后，`render-result.ps1` 才生成供用户阅读的 UTF-8 `result.md`。过期或不合法结果不会进入后续门禁。

角色依赖固定为：开发依赖分析，测试依赖开发，审查同时依赖开发和测试。依赖任务必须完成且已经验证，后续任务才能派发。

## v0.3 任务连接与恢复

每次派发先生成不可变 `dispatch.json`，再由 Codex 原生任务工具发送。状态层记录 `dispatch_id`、消息 ID、读取游标和 `prepared → sent → acknowledged → result_received` 投递状态。结果必须同时匹配任务、窗口和派发编号；恢复命令只给出继续发送、等待确认、等待结果、验证结果或人工检查中的一种确定决策，不自动重复派发。

## v0.4 原生任务适配

总控先预检 `list_threads`、`read_thread`、`send_message_to_thread` 和 `wait_threads`。脚本从派发信封生成不可覆盖的提示词，但不直接调用宿主工具。发送回执与任务观察原样保存在项目状态目录并记录 SHA-256；消息 ID 为可选字段，读取游标是增量等待依据。任务、主机或项目路径不一致时关闭门禁。

## v0.5 结果采集

等待层只定位新 revision、完成 turn 和最终 item，不把可能截断的消息预览当作结果。`import-wait-snapshot.ps1` 将原始 `wait_threads` 响应转换为可审计快照；状态层要求 revision 严格递增，并保存快照与原始响应哈希。

完整结果必须来自新的 `read_thread` 原始响应。`extract-thread-result.ps1` 按等待快照中的 thread、turn 和 item 三重身份提取已完成的 `final_answer`，保留原文并计算 SHA-256。恢复决策在拿到精确结果身份后进入 `fetch_full_result`，完成提取后才进入既有规范化和验证链路。

## v0.6 可信验证门禁

任务不能再通过 `transition -Verified` 自报验证成功。`verify-result` 在状态锁内调用确定性验证器，核对任务、派发、线程、主机、来源消息、项目路径和 Git 修订，并为原始结果及规范化结果生成不可变验证回执。

状态层保存规范化结果与回执的 SHA-256。只有持有有效回执的任务才能进入 `completed`，依赖任务也只接受这种可信完成状态。`audit` 会复核原始结果、规范化结果、回执哈希及相互身份，验证后的证据变化会关闭门禁。

v0.5 及更早版本的历史完成任务若没有回执，升级时保留完成历史但标记为 `legacy-unverified`，并取消后续依赖放行资格。系统不会为旧结果伪造验证证据。

## v0.7 统一动作计划

`plan-next-actions.ps1` 在完整性审计通过后读取整个工作流，为授权、派发、发送、等待、读取、规范化、验证完成和人工检查生成有序动作。多个等待目标按宿主限制自动分成每批最多八个；计划器只描述操作，不调用 Codex 宿主工具。

动作计划绑定 `event_sequence`、`workflow.json` 哈希和 `tasks.json` 哈希。`test-action-plan-current.ps1` 在执行前验证这三个快照依据，任一变化都使计划过期。现有单任务 `reconcile` 保留为兼容入口，工作流级调度以动作计划为准。

## v0.8 真实双窗口验证

真实 Codex 双窗口流程已跑通任务创建、派发、增量等待、完整读取、规范化、可信验证、审计和完成。宿主发送回执可能只有任务 ID，因此消息 ID 和游标继续保持可选，不允许补造；缺失游标时等待参数直接省略。

等待状态新增 `latest_turn_status` 和 `latest_item_phase`。只有 `completed + final_answer` 才能触发完整结果读取；`inProgress + commentary` 始终继续等待。完整验证记录见 `docs/v0.8-real-e2e.md`。

## v0.9 主动回传与断点接管

工作流保存不可变的 `controller_thread_id` 与 `controller_host_id`。每次派发生成唯一 `callback_event_id` 并写入派发信封；执行任务完成工作后、结束本轮前主动调用 `send_message_to_thread` 回传固定 JSON，因此总控无需持续轮询即可被新输入唤醒。

总控不直接信任回传文本。`import-callback-receipt.ps1` 校验工作流、任务、派发、来源线程、目标线程和主机后生成规范化回执，状态层保存其 SHA-256。状态按 `prepared → received → acknowledged` 单向推进；相同事件重复到达保持幂等，不新增事件。总控收到回传后向执行任务发送包含同一事件 ID 的 ACK，并保存宿主工具回执。可信结果验证和回传 ACK 缺一时，任务都不能进入 `completed`。

Codex 桌面宿主运行时已经真实验证可以唤醒等待或 idle 的任务。整个桌面应用退出或电脑离线时，本版本只能依靠持久化状态在宿主恢复后接管，不承诺启动已退出的应用；后台常驻服务仍不在当前范围内。

## v1.0 四角色交付链

结果层新增角色证据：分析确认方案可实施，开发确认批准范围内的实现完成，测试必须提供至少一项真实通过检查，审查必须检查同一修订且不能遗留发现。状态管理器把登记角色和文件归属传给验证器，角色自报不能覆盖总控登记信息。

派发层把每个可信依赖的结束修订固化为 `dependency_revisions`。所有依赖必须汇聚到一个共同修订，并与接收任务的基线相同，从而形成可验证的分析 → 开发 → 测试 → 审查交接。

返修使用新的任务 ID 和 `repair_of` 指向失败来源。状态层只允许一次同角色、同依赖、同授权且不扩大文件范围的定向返修；计划器在存在合法返修时跳过来源失败项，由返修任务继续链路。

## v1.1 总控任期与接管

工作流以 `controller_thread_id + controller_host_id + controller_epoch` 组成总控租约。接管采用项目锁内的比较并交换：请求必须匹配当前身份和任期，成功后任期加一并把旧身份写入只追加语义的 `controller_history`；并发或重复请求因旧任期失效而拒绝。

动作计划 Schema 升级为 v2，并绑定总控身份和任期。任何接管都会同时改变事件序号和状态哈希，因此旧总控不能继续执行旧计划。总控身份不得与执行任务身份重合。

任务在准备派发时固化 `dispatch_controller_epoch` 和回传目标。接管不改写既有派发文件，也不改变既有回传事件；状态层按任务保存的目标验证旧回传。接管后的新任务使用新总控身份。该设计保证审计和幂等，但旧目标完全不可访问时，既有任务仍需通过保存的等待/读取证据恢复，不能伪造回传。

## v1.2 外部动作事务

`external-actions.json` 是派发发送和回传 ACK 的持久化事务账本。每条记录绑定任务、动作类型、总控身份、任期、尝试序号、不可变负载哈希以及工具回执哈希。状态路径为 `prepared → completed`；只有具备核查证据时才能从 `prepared → cancelled` 并创建下一次尝试。

计划器不再直接从待发送状态生成宿主工具调用，而是先生成 `begin_external_action`。登记成功后调用者立即使用返回负载执行工具，再保存回执并完成事务。恢复时，未决 `prepared` 一律进入人工核查，避免在“工具已成功但本地尚未记账”的崩溃窗口盲目重发。

已完成事务与任务状态分两步落账，但不会形成重复发送风险：中断后计划器只生成 `record_external_action`，直接应用已保存回执，不再调用外部工具。动作计划 Schema 升级为 v3 并绑定事务账本哈希；账本变化或旧版计划都会失效。

## v1.3 动作执行租约

`action-executions.json` 为每个被选中的计划动作保存独占认领、控制器任期、逻辑操作哈希、租约期限和结果证据。认领时同时核对计划文件及工作流、任务、外部动作、执行日志四份状态哈希，保证执行者拿到的是当前计划。

认领会使原计划立即失效，但执行者继续使用检查点中保存的不可变动作。重复唤醒或另一个总控实例看到同一逻辑动作处于 `claimed` 或 `failed` 时只能检查现场。租约只允许在原控制器仍有效且未过期时续期；过期和控制器接管都不会被当作“动作没有执行”的证明。

## v1.4 证据化恢复

过期或旧任期认领可以在独立证据支持下进入 `abandoned` 或 `reconciled`。前者表示确认没有产生效果并允许新尝试，后者表示效果已由外部状态证明；活动租约不能被解除。失败执行保留原始失败证据，另行保存用户重试授权和原因消除证据后进入 `retry_authorized`。

每个逻辑操作的尝试号严格递增。计划器把活动认领、可解决认领和失败执行分别映射为检查、证据化解决和请求重试授权。通用恢复层不得覆盖外部发送事务的判断，避免重新打开 v1.2 已关闭的重复投递窗口。

## v1.9.1 项目相邻工作树

工作树默认根目录由项目绝对路径确定：`<项目父目录>\.codex-worktrees\<项目名>`。它与项目处于同一盘符但位于仓库外部，因此不依赖固定盘符，也不会在主仓库产生未跟踪目录。显式配置仍可覆盖默认值，但必须是 Windows 绝对路径，且不能是磁盘根目录、项目本身或项目内部目录。

旧工作流中的空 `git_worktree_root` 在下一次受控状态写入时按相同规则补齐，已有非空配置保持不变。工作树父目录延迟到 Git 创建动作时生成，避免只读或规划阶段在项目外写入目录。路径可以包含 Unicode；实际 Git 创建、状态读取和回执校验共同构成兼容性验证，失败时关闭门禁而不是改写路径。

## v1.9.0 Git 生命周期自动联动

工作流配置保存工作树根目录和目标分支。计划器为已批准的开发任务生成不可变工作树请求，执行回执成功后自动绑定工作树。开发任务不得自行暂存或提交；其交接文件必须精确列出修改文件，总控同时读取 Git 实际状态并与 `allowed_files` 做集合比对，匹配后才进入 `awaiting_commit`。

暂存、提交和合并均复用 `git-actions.json` 事务账本。提交完成时记录开发分支真实提交哈希；修订发布动作只允许更新依赖该开发任务、尚未派发且没有旧工作树绑定的测试或审查任务。测试和审查结果仍通过原有规范化与可信验证链路，合并请求生成器要求两份证据均为 `trusted + passed`，且 `inspected_revision` 与开发提交完全一致。

合并回执单独记录目标分支的 `merge_revision`。提交修订与合并修订不混用，便于测试证据追溯和中断恢复。任何基线漂移、脏工作区、越权文件、证据变化、缺少授权或冲突都会关闭门禁。远程推送、强制操作、冲突解决、工作树和分支删除仍是独立授权边界。

## v1.8.0 Git 事务编排与安装交付

`git-actions.json` 是受控 Git 的持久化事务账本，记录任务、操作、控制器任期、请求路径与哈希、回执及失败证据。状态以 `prepared → completed|failed|cancelled` 单向推进。动作计划绑定账本 SHA-256，只对 `prepared` 生成执行动作，对 `failed` 生成核查动作，从而关闭重复唤醒和中断恢复造成的重复执行窗口。

Git 事务与通用动作执行租约分层：事务记录“是否可能产生 Git 效果”，租约记录“哪个总控正在执行计划动作”。租约过期不能证明 Git 未执行；恢复必须读取仓库和不可变回执。动作计划 Schema 升级为 v7，工作流状态版本升级为 17。

受控 Git 层独立于任务状态机，由不可变请求驱动 `create_worktree → stage → commit → merge`。每次执行重新读取 Git 实际状态，校验仓库根目录、精确基线、分支和授权文件范围，再生成不可覆盖回执。合并门禁要求测试和审查回执哈希均有效且绑定源分支当前提交，并先使用 Git 冲突预检；失败时不进入合并命令。

执行器不包含 push、force、冲突解决和清理操作，因此远程发布及资源删除仍属于独立授权边界。动作计划 Schema 升级为 v6，工作流状态版本升级为 16。

运行期 Skill 继续保持单一自包含目录。仓库级 `installer` 负责安装、升级、卸载和发布打包，不进入 Skill 的提示词上下文，也不改变工作流状态机或任务协议。

安装器先验证 `SKILL.md`、`VERSION`、`agents/openai.yaml`、`scripts` 和 `references`，再把完整来源复制到目标根目录内的唯一暂存目录。已有安装必须显式授权升级，旧目录先移动到 `.toolbbs-backups`，暂存内容校验一致后才切换为正式目录；切换失败时恢复旧目录。同版本且内容一致的重复安装不写入文件。

卸载只接受同时具有 `SKILL.md` 和 `VERSION` 标识的固定 Skill 目录，并默认移动到可恢复备份而非永久删除。打包器在 D 盘 `dist` 生成版本化 ZIP、包内 `PACKAGE_MANIFEST.json` 和独立 SHA-256 文件；产物不进入 Git。普通用户的默认安装根目录遵循 `CODEX_HOME`，未设置时使用用户目录下的 `.codex\skills`，测试必须通过 `-DestinationRoot` 隔离到 D 盘临时目录。
