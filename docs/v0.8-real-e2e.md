# v0.8 真实双窗口端到端验证

验证日期：2026-09-15

## 范围

总控任务在同一 D 盘项目中创建一个只读 Codex 执行任务，真实完成创建、派发、等待、读取、规范化、可信验证、审计和工作流完成。执行任务未修改文件，未执行 Git 写操作，未联系其他任务。

## 实际结果

- `create_thread`：成功，返回真实任务 ID 和 `local` 主机 ID。
- `send_message_to_thread`：成功；宿主只返回任务 ID，没有消息 ID或游标。
- `wait_threads`：成功取得 revision 3 的活动快照和 revision 4 的完成快照。
- `read_thread`：成功按完成 turn 和 `final_answer` item 获取完整结果。
- 结果提取、规范化、`verify-result`、`audit`、可信完成：全部成功。
- 最终动作计划：`workflow_complete`。

原始任务身份和工具响应保存在 Git 忽略的 `.codex-orchestrator/`，不提交公开仓库。

## 发现与修复

1. 发送接口不返回游标时，v0.7 仍生成空字符串 `afterCursor`。v0.8 改为省略该可选参数。
2. 活动 turn 的 commentary 曾被误判为可读取结果。v0.8 保存 turn 状态与 item phase，并且只允许 `completed + final_answer` 进入读取结果动作。

## 未覆盖

- 修改型开发任务、多工作树并行修改和文件冲突。
- 任务失败、取消、需要人工输入及一次定向返修。
- 创建八个以上真实 Codex 任务；批量上限仅由自动化测试覆盖。
- Git 提交、推送、部署和后台常驻运行。
